import { Input, InputNumber, Select, DatePicker } from "antd";
import SelectField from "./select";

export default {
    string: Input,
    number: InputNumber,
    select: SelectField,
    date: DatePicker
};
